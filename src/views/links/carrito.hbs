<div class="container p-4">
    <div class="row">
        <div class="col-md-4">
            <div class="card text-center">
                <div class="card-body">
                    <h3 class="card-title text-uppercase">
                        {{producto.title}}
                     </h3>
                    <p class="m-2">{{producto.precio}}$</p>
                    <p class="m-2">{{producto.description}}</p>
                    <a class="btn btn-outline-danger" href="/vapebro/delete/{{id}}">
                        Delete product
                    </a>
                    <a class="btn btn-outline-secondary" href="/vapebro/edit/{{id}}">
                        Edit
                    </a>
                    <a class="btn btn-outline-secondary fa fa-shopping-cart" href="/vapebro/{{id}}">
                        +
                    </a>
                </div>
            </div>
        </div>